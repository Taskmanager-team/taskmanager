# openapi-typescript declare un peer TypeScript ^5 alors que le projet est en 6.
# C'est un outil CLI de generation, il n'entre pas dans le bundle : on accepte
# la resolution historique pour que `npm ci` passe en CI.
legacy-peer-deps=true
